name: Flutter Web

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: "stable"

      - name: Install dependencies
        run: flutter pub get

      - name: Make envfile
        uses: SpicyPizza/create-envfile@v1.3
        with:
          envkey_API_KEY: ${{ secrets.API_KEY }}
          file_name: .env
          fail_on_empty: false

      - name: Build Web
        run: flutter build web --web-renderer html --base-href='/nasa-apis/' --release

      - name: "Deploy web app"
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: build/web
          github_token: ${{ secrets.GITHUB_TOKEN }}
